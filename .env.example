# OpenAI API 키 (필수) — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# 모델 설정 (기본값으로 두면 됨)
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4o-mini

# 경로
DATA_PATH=data/regulations.jsonl
VECTORSTORE_DIR=vectorstore
